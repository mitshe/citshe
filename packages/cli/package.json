{
  "name": "@citshe/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Thin local CLI for the citshe panel — connect to Claude Code worker sessions running on your VPS.",
  "type": "module",
  "bin": {
    "citshe": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.0.0",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
